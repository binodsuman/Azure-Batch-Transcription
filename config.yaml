speech:
  endpoint: ""
  api_key: ""
  locale: "en-US"

  # Writable container SAS URL -- processed transcripts are uploaded here.
  # NOTE: must have write permission (sp= must include 'w', e.g. sp=rw or sp=rwl)
  destination_container_sas_url: ""

audio:
  # Input audio blob to transcribe (read-only SAS is fine here, sp=r)
  sas_url: ""

polling:
  interval_seconds: 30
  timeout_minutes: 120
